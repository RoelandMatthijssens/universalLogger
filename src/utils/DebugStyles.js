import {StyleSheet} from 'react-native';

export const DebugStyles = StyleSheet.create({
  default: {
    borderWidth: 2,
    borderColor: 'red',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
